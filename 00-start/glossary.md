# 术语表

> TODO：随课程持续补充，按字母/拼音排序。

- **harness**：包裹 LLM 的运行时外壳，负责消息组装、工具执行、日志与生命周期。
- **turn / step**：Turn 是一次用户请求的完整回合；Step 是一次模型请求 + 其工具执行。
- **tool_call**：模型返回的结构化工具调用请求。
- **steering**：在运行中注入/改写消息，影响 agent 下一步行为。
- **golden transcript**：用于回放断言的标准会话日志。
